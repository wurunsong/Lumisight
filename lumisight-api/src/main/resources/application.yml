spring:
  application:
    name: lumisight-api

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

lumisight:
  nebula:
    host: 127.0.0.1
    port: 9669
    username: root
    password: nebula
