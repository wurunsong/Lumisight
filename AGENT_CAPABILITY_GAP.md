# Agent Capability Gap

## Purpose

这份文档用于长期维护 Lumisight Agent 的“当前能力清单 vs Claude Code / Codex 差距分析”。

阅读方式：
- `当前能力`：项目已经具备的能力基线
- `实现标记`：`已实现基础版 / 已有骨架 / 未实现`
- `主要差距`：和成熟代码代理产品相比还缺什么
- `优先级`：`P0 / P1 / P2`
- `实现成本`：`S / M / L`

说明：
- 本文当前聚焦“已经落地的核心能力之后，还缺哪些产品化与工程闭环”。
- 长期记忆系统已经有第一版实现，后续关注点转向自动沉淀、治理与评测。
- 成本评估偏工程实现成本，不是纯研究难度。

## Current Capability Baseline

当前已经具备的核心能力：

- 会话级调度：`FOLLOW / COLLECT / STEER`
- 会话控制：`interrupt / resume / HUMAN_GATE`
- 多轮编排：`tool / ask_user / final`
- 任务清单：`todo_write`
- Skill 路由与 Skill 约束注入
- 本地文件、Git、Java 编译 / lint / LSP 工具
- KG / Vector / 方法源码增强工具
- 浏览器 / DOM 工具
- 上下文账本、artifact 落盘、Snip / Micro-Compact / Projection / Auto-Compact
- 四分类长期记忆与相关记忆召回
- Sandbox、快照、回滚、Hook
- SSE / WebSocket 会话级事件流
- Cron 定时触发 Agent 任务
- 跨会话持久化任务系统
- BUG_FIX 编译 / lint 自修复闭环
- `task_subagent` 干净子任务、team 执行协议、执行状态持久化与波次级 resume

一句话概括：

> 现在更像“已经能工作的工程型 Agent 内核”，但还不是“完整产品态的开发代理系统”。

## Gap Ranking

我重新按“对当前 Lumisight 最重要、最能影响真实交付能力”的标准排了一次顺序。判断原则是：

- 先补能让单 Agent 持续稳定干活的能力
- 再补能让复杂任务更可控、更可验证的能力
- 最后再补产品化和长期演进层能力

| 排名 | 能力 | 当前能力 | 现在最该补的点 | 状态 |
|---|---|---|---|---|
| 1 | 计划执行系统 | 已有 `todo_write` 与 `task_*` 持久化任务图 | 自动续跑、完成判定、环检测、release 回退、任务看板 | `已实现基础版` · `P0` · `M` |
| 2 | 自修复闭环 | 已支持写代码后自动编译 / lint 并回灌失败结果 | 集成测试、服务启动检查、页面行为验证 | `已实现基础版` · `P1` · `M` |
| 3 | 文件编辑工作流 | 能读写、回滚、走工具链 | 大文件局部编辑、多文件原子变更、编辑后验证闭环 | `已实现基础版` · `P1` · `M` |
| 4 | 观测与审计 | 已有事件流、cron 历史基础 | token/cost/latency、工具统计、压缩统计、审计面板 | `已实现基础版` · `P1` · `M` |
| 5 | 权限治理 | 已有 sandbox、人审门控、快照 | 更细粒度授权、风险分级、用户可见权限解释、最小权限规划 | `已实现基础版` · `P1` · `M` |
| 6 | 环境操作 | 能做文件、Git、Java 工具调用 | 依赖安装、服务启动、日志检查、CI 修复 | `已实现基础版` · `P1` · `M` |
| 7 | 浏览器 / DOM 操作 | 已支持网页打开、DOM 摘要、点击输入、截图与会话级复用 | 导航 / 等待策略、登录态治理、trace / video、页面断言模板 | `已实现基础版` · `P1` · `M` |
| 8 | 多 Agent | 已支持 `MULTI_AGENT`、`task_subagent`、team 协议、拓扑、并发、resume | planner、team 共享工作台、fan-in 质量、长期角色化、端到端验证 | `已实现基础版` · `P1` · `L` |
| 9 | RAG / 图谱评测 | 已接入 KG / Vector / 方法源码增强工具 | 评测基线、数据集验证、召回 / 命中率 / 答案质量闭环 | `已实现基础版` · `P1` · `M` |
| 10 | 长期记忆 | 已支持四分类记忆、MEMORY 索引、相关记忆召回与陈旧度提醒 | 自动沉淀、冲突消解、编辑 / 删除治理、质量评测、写入审计 | `已实现基础版` · `P2` · `M` |
| 11 | Artifact 体系 | 上下文大结果可落盘 | 报告、补丁、测试摘要、交付物索引管理 | `已实现基础版` · `P2` · `M` |
| 12 | 前端产品体验 | 有调试页、SSE / WS 基础 | job / session / history / cron / tool trace / context 可视化 | `已实现基础版` · `P2` · `L` |
| 13 | Prompt / 策略治理 | 有 Skill、Verifier、决策主链 | prompt versioning、A/B 验证、回归基线 | `已实现基础版` · `P2` · `M` |
| 14 | 模型适配层 | 已有上下文阈值可配置 | 按模型自动适配窗口、压缩策略、输出预算模板 | `已实现基础版` · `P2` · `S` |
| 15 | 提示词缓存 | 已有上下文压缩、技能按需注入基础 | system prompt / 子任务 prompt / fan-in 模板缓存复用 | `已实现基础版` · `P2` · `S` |
| 16 | 梦境 / 离线反思 | 目前只有在线执行链路 | 低峰期复盘、失败归因、经验蒸馏、候选任务回灌 | `未实现` · `P2` · `M` |
| 17 | 自进化 | 当前主要靠人工调 prompt / tool / workflow | 基于观测、评测和历史结果的自适应优化与技能提案 | `未实现` · `P2` · `L` |

## Priority Interpretation

### Top 3

这三项我现在认为最重要：

1. `计划执行系统`
2. `自修复闭环`
3. `文件编辑工作流`

原因：

- 它们决定 Agent 能不能把一个复杂开发任务稳定拆开、持续推进，并最终把改动真正落到仓库里。
- 如果这三项不稳，后面无论是多 Agent、RAG 还是长期记忆，都只是在放大主链路的不稳定。

### Middle Layer

这一层决定 Agent 能不能像“成熟开发搭档”一样可控、可验证、可定位问题：

- `观测与审计`
- `权限治理`
- `环境操作`
- `浏览器 / DOM 操作`
- `多 Agent`
- `RAG / 图谱评测`

其中需要特别注意：

- `浏览器 / DOM 操作`、`自修复闭环`、`长期记忆` 已经从“缺失能力”进入“基础版已实现，继续补闭环”的阶段。
- `多 Agent` 已经从“骨架”进入“基础版已实现”阶段，但当前更准确地说是“请求内 Lead 编排 + team 协议执行器”，还不是长期自治的多角色系统。

## Multi-Agent Specific Gap

如果只看多 Agent，本项目现在已经不是“有没有”问题，而是“基础版之后还差哪些产品化与协作闭环”。

### 已经完成到哪一层

- `Phase 1`：`task_subagent` 已经完成。子 Agent 有独立上下文、最小只读权限、深度/轮次/超时约束，且不持有最终写仓库和最终回答权限。
- `Phase 2`：team execution protocol 已有基础版。Lead/team mailbox、权限申请闭环、task state、execution-state 持久化、wave 边界恢复都已落地。
- `Phase 3`：topology + orchestration 已有基础版。`SERIAL_DAG / FAN_OUT_FAN_IN / HYBRID`、同 wave 并发执行、失败依赖阻断和主会话收口都已经接入主链路。

### 还缺的核心内容

1. 更强的 planner 与升级判定

- 现在更多还是启发式规则和关键词信号。
- 还缺基于 repo 状态、工具可用性、历史失败率、任务类型和验证成本的更强 orchestration planner。
- 这会直接决定 task 拆解质量、拓扑选择质量，以及“该不该升级多 Agent”。

2. 长期存活的角色化 worker

- 现在的 worker 本质上仍是“请求内临时执行者”。
- 还缺跨请求长期存在的 role identity、角色偏好、角色级工作记忆和异步待命机制。
- 这也是当前体系为什么更像“请求内 Lead 编排系统”，而不是长期自治团队。

3. team 级共享工作台

- 现在已有 execution state，但 team 共享 scratchpad / artifact / evidence 汇聚层还不完整。
- 还缺让多个 worker 能围绕同一批中间证据持续协作、复用和 fan-in 的共享工作面。
- 否则很多跨任务协作仍然只能靠 Lead 的最终收口阶段做二次整理。

4. fan-in 冲突消解与证据排序

- 现在能并发做、也能回流，但汇总阶段仍偏轻。
- 还缺“结果互相冲突时怎么裁决”“不同 worker 的证据怎么排序”“谁的结论可信度更高”的系统化策略。
- 这部分决定多 Agent 不是只会并发，而是真能提升最终答案质量。

5. 权限解释与审批策略深化

- 现在已有 permission request/response 闭环，但策略颗粒度还不够细。
- 还缺对“为什么这个 worker 需要这项权限、风险等级是什么、有没有更小授权替代”的可解释审批面。
- 不补这一层，多 Agent 一旦更强，就更容易把风险一起放大。

6. 端到端集成验证

- 现在已经有 focused test，能覆盖 team lifecycle、依赖、恢复和并发行为。
- 但还缺更长链路的真实端到端验证，例如：主会话升级判断 -> planner -> 多 wave 并发 -> 权限申请 -> interrupt/resume -> fan-in -> 最终回答。
- 这部分是多 Agent 从“能跑”走向“可稳定托付”的关键。

### 推荐的多 Agent 下一步顺序

1. 先补 planner 质量，让拆解、拓扑和升级判定更稳定。
2. 再补 team 级共享 scratchpad / artifact / evidence 汇聚层。
3. 然后加强 fan-in 冲突消解与证据排序。
4. 再扩多 Agent 端到端集成验证，覆盖真实主链路与恢复链路。
5. 最后再把角色长期化、异步待命和跨请求自治能力做深。

### Productization Layer

这一层更多提升产品化程度、复用效率和长期演进能力：

- 长期记忆
- Artifact 体系
- 前端产品体验
- 模型适配层
- Prompt/策略治理
- 梦境 / 离线反思
- 自进化
- 提示词缓存

## Recommended Build Order

建议按下面顺序推进：

1. 更完整的计划执行系统
2. 自动测试 / 集成验证 / 修复闭环
3. 文件编辑工作流完善
4. 观测与审计
5. 权限治理增强
6. 环境操作闭环
7. 浏览器 / DOM 操作能力
8. 多 Agent 产品化深化
9. RAG / 图谱评测体系
10. 长期记忆治理与评测
11. Artifact 体系完善
12. 前端产品化体验
13. Prompt / 策略治理
14. 模型适配层
15. 提示词缓存
16. 梦境 / 离线反思
17. 自进化

## Why This Order

### 1. 计划系统先做

原因：

- 当前已经有会话内 todo 和跨会话 task 基础，但还缺自动续跑、完成判定和更稳定的阻塞编排
- 复杂工程任务先把任务骨架打稳，后续测试 / 验证闭环才更容易持续执行

### 2. 多层自修复闭环第二

原因：

- 当前已经覆盖编译 / lint 级自动回灌，但还缺测试、服务与页面级验证
- 继续补齐后，Agent 才能更稳定地把“改完代码”推进到“确认可用”

### 3. 文件编辑工作流第三

原因：

- Agent 最终能不能稳定改仓库，不只取决于会不会写，而取决于能不能安全地做局部编辑、协调多文件变更，并把验证闭环接上。
- 如果编辑工作流还粗糙，计划系统和自修复就会频繁在“最后一公里”翻车。

### 4. 观测与审计第四

原因：

- 当能力越来越复杂，没有观测就很难调优
- 上下文压缩、cron、工具失败、模型成本都需要被看见

### 5. 权限治理第五

原因：

- 当 Agent 的文件写入、浏览器操作、多 Agent 协作越来越强时，权限解释和最小授权边界会直接决定这套系统能不能放心用。
- 权限治理做得晚，后面很多能力都只能“先跑起来”，很难放心产品化。

### 6. 环境操作与浏览器能力要尽早补齐

原因：

- 很多真实开发任务不是只改本地代码，还要装依赖、拉起服务、看日志、打开页面、识别 DOM、点按钮、看渲染结果。
- 这两类能力补齐后，Agent 才更接近“能自己验证工程结果”的完整开发搭档。

### 7. 多 Agent 排在中后段深化

原因：

- 如果计划系统、自修复、编辑工作流、审计和权限都还没打稳，多 Agent 很容易只是“把不稳定放大”。
- 现在基础版已经落地，下一阶段更应该补“planner 质量、team 共享工作台、fan-in 质量、角色长期化和集成验证”，而不是继续无限扩张拓扑名词。

### 8. 长期记忆治理后置，但不能忘

原因：

- 记忆系统第一版已经落地，但“自动写什么、什么时候不该写、怎么删改和评测”仍然更适合建立在单 Agent 执行闭环稳定之后
- 如果计划系统、自修复、审计都还没打稳，记忆层只会更快放大脏状态和错误状态

### 9. RAG / 图谱要先补评测，再谈增强

原因：

- 现在能力虽然已经接进来了，但“能调用”不等于“有效”
- 如果没有开源数据集和稳定评测基线，后面很难判断召回策略、融合策略和答案质量到底有没有提升

## Short Version

如果只看一句话：

> 当前 Lumisight Agent 已经具备“会话级、多轮、可用工具、可控写入、可压缩上下文、可定时触发”，并且浏览器 / DOM、长期记忆、持久化任务、自修复和多 Agent 协作都已有第一版落地；短期最值得补齐的差距，集中在自动修复深化、计划系统完善、观测审计、RAG / 图谱评测、权限治理，以及把当前请求内多 Agent 继续推进成“planner 更强、team 共享更完整、fan-in 更可靠、验证更扎实”的协作系统。
