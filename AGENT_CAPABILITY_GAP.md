# Agent Capability Gap

## Purpose

这份文档用于维护 Lumisight Agent 的“当前能力清单 vs 还值得继续补的点”。

阅读方式：
- `当前能力`：项目已经具备的能力基线
- `状态`：`已实现基础版 / 未实现`
- `建议动作`：`建议继续做 / 可以先不做 / 完全可以不做`

说明：
- 本文按“学习 / 个人项目 / 找工作展示”的目标来评估，不按企业级产品化标准要求自己。
- 重点不是把每一项都做满，而是判断哪些能力已经足够拿来讲，哪些还值得补一点，哪些可以直接收尾。

## Current Capability Baseline

当前已经具备的核心能力：

- 会话级调度：`FOLLOW / COLLECT / STEER`
- 会话控制：`interrupt / resume / HUMAN_GATE`
- 多轮编排：`tool / ask_user / final`
- 任务清单：`todo_write`
- Skill 路由与 Skill 约束注入
- 本地文件、Git、Java 编译 / lint / LSP 工具
- 依赖安装、服务启动、后台日志检查
- KG / Vector / 方法源码增强工具，以及 CodeSearchNet 风格 RAG 检索评测
- 浏览器 / DOM 工具
- 上下文账本、artifact 落盘、Snip / Micro-Compact / Projection / Auto-Compact
- 四分类长期记忆与相关记忆召回
- Sandbox、快照、回滚、Hook
- SSE / WebSocket 会话级事件流
- Cron 定时触发 Agent 任务
- 基于 task / memory / cron 信号的离线反思与 project memory 沉淀
- 跨会话持久化任务系统
- BUG_FIX 编译 / lint 自修复闭环
- `task_subagent` 干净子任务、team 执行协议、执行状态持久化与波次级 resume

一句话概括：

> 现在更像“已经能工作的工程型 Agent 内核”，但还不是“完整产品态的开发代理系统”。

## Gap Ranking

我重新按“对学习 / 个人项目展示最重要”的标准排了一次顺序。判断原则是：

- 先看会不会影响你现场讲解和演示
- 再看会不会影响项目完整度
- 最后才看产品化长尾能力

| 排名 | 能力 | 当前能力 | 现在最该补的点 | 状态 |
|---|---|---|---|---|
| 1 | 多 Agent | 已支持 `MULTI_AGENT`、`task_subagent`、team 协议、拓扑、并发、`resume`，并补齐自动升级信号、编排说明与控制台事件展示 | 现在已经够展示；把“请求内 Lead 编排系统”的能力边界讲清楚即可，`planner`、长期角色化、team 共享工作台可先不做 | `已实现基础版` · `可以先收尾` |
| 2 | 计划执行系统 | 已有 `todo_write`、`task_create / list / get / claim / release / complete / resume / board`，并补齐依赖环检测 | 现在已经够展示；自动调度、产品化看板 UI 可以先不做 | `已实现基础版` · `可以先收尾` |
| 3 | 自修复闭环 | 已支持写代码后自动编译 / lint 并回灌失败结果 | 再补服务启动检查或页面级验证会更好，但不是必须 | `已实现基础版` · `建议继续做少量收尾` |
| 4 | 浏览器 / DOM 操作 | 已支持网页打开、DOM 摘要、点击输入、截图与会话级复用 | 如果本地演示稳定，trace / video / 登录态治理都可以先不做 | `已实现基础版` · `可以先收尾` |
| 5 | 文件编辑工作流 | 能读写、回滚、走工具链 | 大文件局部编辑和多文件原子性属于增强项，不是简历展示硬要求 | `已实现基础版` · `可以先收尾` |
| 6 | 环境操作 | 已支持文件、Git、Java 工具调用，并补齐 `envInstall / serviceStart / serviceStatus / serviceLogs / serviceStop` | 现在已经够展示；更深的进程编排、端口探测和多服务依赖治理可先不做 | `已实现基础版` · `可以先收尾` |
| 7 | 长期记忆 | 已支持四分类记忆、MEMORY 索引、相关记忆召回与陈旧度提醒 | 作为亮点已经够讲；自动沉淀和治理可先不做 | `已实现基础版` · `可以先收尾` |
| 8 | RAG / 图谱评测 | 已接入 KG / Vector / 方法源码增强工具，并补齐 `CodeSearchNet` 风格代码检索评测入口 | 现在已经够展示；更重的图谱评测、对比实验平台和大规模基准管理可以先不做 | `已实现基础版` · `可以先收尾` |
| 9 | 观测与审计 | 已有事件流、cron 历史基础 | 审计面板、token/cost 统计对个人项目不是刚需 | `已实现基础版` · `可以先不做` |
| 10 | 权限治理 | 已有 sandbox、人审门控、快照 | 现在这层已经够讲，细粒度风险分级可以不做 | `已实现基础版` · `可以先不做` |
| 11 | Artifact 体系 | 上下文大结果可落盘 | 报告、补丁、交付物索引是加分项，但不影响主线展示 | `已实现基础版` · `可以先不做` |
| 12 | 前端产品体验 | 有调试页、SSE / WS 基础 | 调试页能用就行，不必做完整产品界面 | `已实现基础版` · `可以先不做` |
| 13 | Prompt / 策略治理 | 有 Skill、Verifier、决策主链 | versioning、A/B、回归基线都偏平台化，可不做 | `已实现基础版` · `完全可以不做` |
| 14 | 模型适配层 | 已有上下文阈值可配置 | 自动按模型适配属于长尾优化，可不做 | `已实现基础版` · `完全可以不做` |
| 15 | 提示词缓存 | 已有上下文压缩、技能按需注入基础 | 缓存命中策略对个人项目展示价值不高，可不做 | `已实现基础版` · `完全可以不做` |
| 16 | 梦境 / 离线反思 | 已支持基于 `task / memory / cron` 信号生成离线反思摘要，默认沉淀为 `project memory`，并可选创建 follow-up task | 现在这个版本已经够讲；自治研究型“做梦”系统不必继续深挖 | `已实现基础版` · `可以先收尾` |
| 17 | 自进化 | 当前主要靠人工调 prompt / tool / workflow | 这更像长期产品 / 研究方向，当前完全可以不做 | `未实现` · `完全可以不做` |

## Priority Interpretation

### 最值得收尾的几项

这几项最值得再补一点，因为它们最影响你对外展示：

1. `多 Agent`
2. `计划执行系统`
3. `自修复闭环`
4. `浏览器 / DOM 操作`

原因：

- 它们最容易被面试官、同学或开源读者一眼看见。
- 也是最能体现“这个项目不是玩具 demo，而是有完整 agent 主线”的地方。
- 但这里说的“收尾”是把演示链路和讲解口径做顺，不是把它们做成企业级平台。
- 其中 `多 Agent` 这项现在已经基本收顺，后面更多是保持文档口径和现场演示稳定。

### 可以先收尾的

这些能力已经够用了，继续深挖收益没有前几项高：

- `文件编辑工作流`
- `环境操作`
- `长期记忆`
- `RAG / 图谱评测`

其中需要特别注意：

- `长期记忆`、`RAG / 图谱` 这类能力对个人项目更像“亮点”，不是必须做全治理和评测。
- `文件编辑工作流`、`环境操作` 只要不明显拖后腿，就不用继续无限打磨。

## Multi-Agent Specific Gap

如果只看多 Agent，这里更适合按“展示项目还差哪些收尾”来判断，而不是按完整产品化来要求自己。

### 已经完成到哪一层

- `Phase 1`：`task_subagent` 已经完成。子 Agent 有独立上下文、最小只读权限、深度/轮次/超时约束，且不持有最终写仓库和最终回答权限。
- `Phase 2`：team execution protocol 已有基础版。Lead/team mailbox、权限申请闭环、task state、execution-state 持久化、wave 边界恢复都已落地。
- `Phase 3`：topology + orchestration 已有基础版。`SERIAL_DAG / FAN_OUT_FAN_IN / HYBRID`、同 wave 并发执行、失败依赖阻断和主会话收口都已经接入主链路。

### 还值得补的内容

1. 更强的 planner 与升级判定

- 现在已经补到“启发式升级信号 + 置信度 + 命中信号解释”，但本质上仍偏轻量。
- 还缺基于 repo 状态、工具可用性、历史失败率、任务类型和验证成本的更强 orchestration planner。
- 这会直接决定 task 拆解质量、拓扑选择质量，以及“该不该升级多 Agent”。

2. 长期存活的角色化 worker

- 现在的 worker 本质上仍是“请求内临时执行者”。
- 还缺跨请求长期存在的 role identity、角色偏好、角色级工作记忆和异步待命机制。
- 但对当前项目来说，这一项完全可以只在文档里说明“暂未做”，不必真的实现。

3. team 级共享工作台

- 现在已有 execution state，但 team 共享 scratchpad / artifact / evidence 汇聚层还不完整。
- 还缺让多个 worker 能围绕同一批中间证据持续协作、复用和 fan-in 的共享工作面。
- 否则很多跨任务协作仍然只能靠 Lead 的最终收口阶段做二次整理。
- 这一项对长期演进很重要，但对当前找工作展示不是硬门槛。

4. fan-in 冲突消解与证据排序

- 现在能并发做、也能回流，并且已经补了 plan narrative、task brief、boundary notes 这类解释信息，但汇总阶段仍偏轻。
- 还缺“结果互相冲突时怎么裁决”“不同 worker 的证据怎么排序”“谁的结论可信度更高”的系统化策略。
- 这部分决定多 Agent 不是只会并发，而是真能提升最终答案质量。

5. 权限解释与审批策略深化

- 现在已有 permission request/response 闭环，但策略颗粒度还不够细。
- 还缺对“为什么这个 worker 需要这项权限、风险等级是什么、有没有更小授权替代”的可解释审批面。
- 不补这一层，多 Agent 一旦更强，就更容易把风险一起放大。
- 但对个人项目来说，现在已有 sandbox / human gate / 快照，已经够讲。

6. 端到端集成验证

- 现在已经有 focused test，能覆盖 team lifecycle、依赖、恢复和并发行为。
- 但还缺更长链路的真实端到端验证，例如：主会话升级判断 -> planner -> 多 wave 并发 -> 权限申请 -> interrupt/resume -> fan-in -> 最终回答。
- 这部分是多 Agent 从“能跑”走向“可稳定托付”的关键。
- 但你已经明确自己手测，所以这里不必为了文档再把它拔高成必须项。

### 推荐的多 Agent 下一步顺序

1. 继续保持文档里的边界表达清楚，明确这是“请求内 Lead 编排系统”。
2. 如果还想补一点，就优先补 planner 质量，让拆解、拓扑和升级判定更稳定。
3. 再考虑 fan-in 收口质量，保证最终结论更像样。
4. 如果还有精力，再考虑 team 共享 scratchpad / artifact 汇聚层。
5. 长期角色化、异步待命和跨请求自治能力可以留作 future work。

### 可以先不做 / 完全可以不做

这一层更多是产品化、平台化或研究化方向，对当前项目并不构成必须项：

- 长期记忆
- Artifact 体系
- 前端产品体验
- 模型适配层
- Prompt/策略治理
- 自进化
- 提示词缓存

## Recommended Build Order

建议按下面顺序推进：

1. 把 `多 Agent` 视为已可收尾能力，重点保持展示链路和文档口径稳定
2. 把 `计划执行系统` 作为基础闭环收尾
3. 视情况给 `自修复闭环` 或 `浏览器 / DOM` 再补一个更顺手的演示点
4. 其余能力以文档说明边界为主，不强求继续实现

## Why This Order

### 1. 多 Agent 先收口

原因：

- 这是你项目里最有辨识度、最适合讲故事的亮点之一。
- 现在已经能比较完整地演示了，后面重点就是“边界清楚、演示顺畅、架构图好懂”。

### 2. 计划系统第二

原因：

- 现在已经补到一个不错的基础闭环了，足够作为“我不只是做了 todo，还有跨会话任务系统”的能力点来讲。
- 后面没必要再往自动调度平台方向做很深。

### 3. 自修复和浏览器能力第三

原因：

- 这两项都很适合做演示：一个体现“能自我验证”，一个体现“能操作页面”。
- 补一点点体验就能明显加分，但不用做成重型系统。

### 4. 其余大多是“够讲就行”

原因：

- 观测、权限、RAG 评测、长期记忆治理、产品界面、prompt versioning、模型适配、提示词缓存这些东西，放在企业产品里很重要。
- 但放在当前这个项目里，只要你知道它们是什么、为什么没继续做，就已经足够了。

## Short Version

如果只看一句话：

> 当前 Lumisight Agent 已经具备“会话级、多轮、可用工具、可控写入、可压缩上下文、可定时触发”，并且浏览器 / DOM、长期记忆、持久化任务、自修复、离线反思和多 Agent 协作都已有第一版落地；如果按“学习 / 个人项目 / 找工作展示”的标准看，现在更适合做的是少量收尾、把亮点讲清楚，而不是继续把每个方向都做成企业级产品能力。
